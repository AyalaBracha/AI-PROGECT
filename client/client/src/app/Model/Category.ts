export interface Category {
  id: number;
  name: string;
  image: string;  // אייקון או תמונה של הקטגוריה
}

export interface CategoryDTO {
  name: string;
  image: string;  // אייקון או תמונה של הקטגוריה
}

